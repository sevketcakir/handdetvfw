#ifndef BGCOLOR
#define BGCOLOR

class bgColor {
public:
unsigned char Intensity;
unsigned int Count;
bgColor();
bgColor(unsigned char intensity,unsigned int count);
bgColor(unsigned char intensity);
};
#endif
